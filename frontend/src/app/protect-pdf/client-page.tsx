"use client";
import { useState } from "react"; import { ShieldCheck } from "lucide-react"; import PdfOperationShell from "@/components/pdf-tools/PdfOperationShell";
export default function Client(){const [password,setPassword]=useState(""); const [confirm,setConfirm]=useState(""); const [printing,setPrinting]=useState(false); const ok=password.length>=4&&password===confirm; return <PdfOperationShell title="PDF 加密" description="设置打开密码，并可限制打印与修改。" icon={<ShieldCheck className="w-8 h-8"/>} gradient="" endpoint="protect" outputSuffix="-protected.pdf" fields={{password,keyLength:256,preventPrinting:String(printing),preventModify:"false"}} canSubmit={ok} validationMessage={!ok&&password?"密码至少4位，且两次输入必须一致":undefined}><div className="grid gap-4 md:grid-cols-2"><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="设置打开密码" className="rounded-xl border border-theme bg-theme-secondary p-3 text-theme"/><input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="再次输入密码" className="rounded-xl border border-theme bg-theme-secondary p-3 text-theme"/></div><label className="mt-4 flex gap-3 text-theme"><input type="checkbox" checked={printing} onChange={e=>setPrinting(e.target.checked)}/>禁止打印</label></PdfOperationShell>}