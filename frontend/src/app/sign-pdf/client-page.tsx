"use client";
import { useState } from "react"; import { PenTool } from "lucide-react"; import PdfOperationShell from "@/components/pdf-tools/PdfOperationShell";
export default function Client(){const [signature,setSignature]=useState<File|null>(null); const [x,setX]=useState(40); const [y,setY]=useState(700); const [every,setEvery]=useState(false); return <PdfOperationShell title="PDF 在线签名" description="上传透明背景签名图片，并按 PDF 点坐标放置。" icon={<PenTool className="w-8 h-8"/>} gradient="" endpoint="sign" outputSuffix="-signed.pdf" fields={{x,y,everyPage:String(every)}} extraFiles={{signature}} canSubmit={!!signature} validationMessage={!signature?"请上传签名图片":undefined}><div className="grid gap-4 md:grid-cols-3"><label className="rounded-xl border border-dashed border-theme p-4 text-sm text-theme">签名图片<input type="file" accept="image/png,image/jpeg,image/svg+xml" onChange={e=>setSignature(e.target.files?.[0]||null)} className="mt-2 block w-full"/></label><label className="text-sm text-theme">X 坐标<input type="number" min="0" value={x} onChange={e=>setX(Number(e.target.value))} className="mt-2 w-full rounded-xl border border-theme bg-theme-secondary p-3"/></label><label className="text-sm text-theme">Y 坐标<input type="number" min="0" value={y} onChange={e=>setY(Number(e.target.value))} className="mt-2 w-full rounded-xl border border-theme bg-theme-secondary p-3"/></label></div><label className="mt-4 flex gap-3 text-theme"><input type="checkbox" checked={every} onChange={e=>setEvery(e.target.checked)}/>应用到每一页</label></PdfOperationShell>}